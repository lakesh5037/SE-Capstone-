# Frontend Production Environment
# This file is read by Vite at BUILD time (inside Docker) and bakes the URL into the JS bundle.
# The browser will call this URL — so this must be the HOST machine's port (localhost:8081),
# NOT the Docker internal network name (hemoscan-backend).
VITE_API_URL=http://localhost:8081/
